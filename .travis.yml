sudo: false
language: java
jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7

cache:
  directories:
    - $HOME/.m2

install: true

script:
  - jdk_switcher use oraclejdk8
  - export JAVA8_PLUS_HOME=$JAVA_HOME
  - jdk_switcher use $TRAVIS_JDK_VERSION
  - ./mvn-build-all.sh
