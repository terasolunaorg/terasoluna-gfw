sudo: false
language: java
jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7

cache:
  directories:
    - $HOME/.m2

install: true

script:
  - mvn -U clean install
  - mvn -U -P common-libs-only jacoco:report coveralls:report
